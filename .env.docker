# Docker Development (με docker-compose)
# ΤΑ ΠΕΡΙΣΣΟΤΕΡΑ ΟΡΙΖΟΝΤΑΙ ΣΤΟ docker-compose.yml
# Αυτό το αρχείο είναι για reference

# Το docker-compose.yml ορίζει:
# DATABASE_URL=postgres://user:password@db:5432/aade_db
# ENVIRONMENT=development

# Αν θέλεις να το χρησιμοποιήσεις:
# docker-compose --env-file .env.docker up
DATABASE_URL=postgres://user:password@db:5432/aade_db
SERVER_ADDR=0.0.0.0:3000
ENVIRONMENT=development
RUST_LOG=info,aade_validator=debug
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:5174
